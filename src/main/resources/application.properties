# Server Configuration
server.port=8080

# Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://coachkonnects-db.c9im2o0s2pod.ap-south-1.rds.amazonaws.com:5432/postgres?schema=public
spring.datasource.username=postgres
spring.datasource.password=JdajE7WqB7G6X9B
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
# update allows Hibernate to automatically create/update tables without destroying data
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Run SQL migration on startup to fix student_id constraint
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:db-migration.sql

# JavaMailSender Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=coachkonnects@gmail.com
spring.mail.password=keba tjff bdgj jxpq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Global CORS Configuration
cors.allowed-origins=http://localhost:8080,http://localhost:8081,http://localhost:5173,http://localhost:5174

# Production Domain Configuration
# Change this to the client's actual website domain when deploying (e.g. coachkonnects.com)
app.domain=localhost

# Frontend Application URL (Used for generating email links)
# For production, change this to the live React domain (e.g., https://coachkonnects.com)
app.frontend.url=http://localhost:5173
